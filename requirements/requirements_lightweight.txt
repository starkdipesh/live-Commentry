# Minimal dependencies for low-spec PC

# Core (REQUIRED - lightweight)
mss>=10.0.0              # Fast screen capture
Pillow>=10.0.0           # Image processing (basic)

# TTS (FREE, lightweight)
edge-tts>=6.1.0          # Natural voice

# Audio (lightweight)
pygame>=2.0.0            # Audio playback

# HTTP
requests>=2.28.0         # Ollama API

# For future training (OPTIONAL - install only when needed)
# transformers>=4.30.0   # Model training
# datasets>=2.12.0       # Dataset handling
# peft>=0.4.0           # LoRA training
